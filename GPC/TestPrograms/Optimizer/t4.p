program boo( input, output );
 var x, y, z: integer;
   procedure baz(x: integer);
   begin
     write(x)
    end;
begin
  baz(50 * 2 + (5 - 3))
  // y := 10 * 10 + 25 * 4;
  //z := x / 2;
  // z := 10 / -2;

 // write(x);
 // write(y);
 // write(z)
end.
